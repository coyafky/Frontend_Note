let createdBoolean: Boolean = new Boolean(1);
let booleand: boolean = true;
let booleand2: boolean = Boolean(1);

let isDone: boolean = true;
let isNotDone: boolean = false;

// 如果换成大写

let isDone2: Boolean = true; // Boolean 和 boolean 有什么区别
console.log(isDone2);
